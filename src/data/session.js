// The signed-in planner shown at the foot of the sidebar.
export const CURRENT_USER = {
  name: "Miguel Jackson",
  role: "Administrator",
};

// Prototype-only values used to prefill the sign-in form so the walkthrough can
// be clicked through without typing. Not a real account.
export const DEMO_CREDENTIALS = {
  email: "admin@company.com",
  password: "password10",
};
